import React from "react";

const Header = () => {
  return (
    <div className="headerComponent">
      <button>Menu</button>
      <h1>Hangman</h1>
      <button>Settings</button>
    </div>
  );
};

export default Header;
